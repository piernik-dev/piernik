! $Id$
    idna(ifluid)=idnd(iter)
    imxa(ifluid)=imxd(iter)
    imya(ifluid)=imyd(iter)
    imza(ifluid)=imzd(iter)

    adust((iter-1)*nudpf+1)=idnd(iter)
    adust((iter-1)*nudpf+2)=imxd(iter)
    adust((iter-1)*nudpf+3)=imyd(iter)
    adust((iter-1)*nudpf+4)=imzd(iter)

    findex(ifluid)=nudpf
    fdust(idust)=ifluid
    fluidtype = 'isothermal'
    gamma(ifluid) = 1.0

    write(*,'(a6,i2,a11,a10)') 'fluid ',ifluid,' dust      ',fluidtype
