! $Id$
    idna(ifluid)=idni(iter)
    imxa(ifluid)=imxi(iter)
    imya(ifluid)=imyi(iter)
    imza(ifluid)=imzi(iter)

    aions((iter-1)*nuipf+1)=idni(iter)
    aions((iter-1)*nuipf+2)=imxi(iter)
    aions((iter-1)*nuipf+3)=imyi(iter)
    aions((iter-1)*nuipf+4)=imzi(iter)

    findex(ifluid)=nuipf
    magn(ifluid)=1
    fiond(ifion)=ifluid
    fmagn(imagn)=ifluid
    ifion=ifion+1
    imagn=imagn+1

#ifdef ISO
    fluidtype = 'isothermal'
    gamma(ifluid) = 1.0
#else /* ISO */
    iena(ifluid)=ieni(iter)
    aions((iter-1)*nuipf+5)=ieni(iter)
    fadiab(iadiab)=ifluid
    iadiab=iadiab+1
    fluidtype = 'adiabatic'
#endif /* ISO */
    write(*,'(a6,i2,a11,a10)') 'fluid ',ifluid,' ionized   ',fluidtype
