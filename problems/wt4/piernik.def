#define HDF5
#define IONIZED
#undef  MAGNETIC

#define WT4 /* unit system */

#define ISO
#define ISO_LOCAL

#define GLOBAL_FR_SPEED

#define VANLEER

#define GRAV
#define GRAV_PTMASSPURE
#define MULTIGRID

#if __GNUC__ >=4 && __GNUC_MINOR__ >=5
#define NEW_HDF5
#else
#undef NEW_HDF5
#endif

#if 0
  __NO_FLUID_STEP is a special flag that switches off fluid evolution.
  It is useful for solving static problems (mostly testcases).
#endif
#undef  __NO_FLUID_STEP

#if 0
  USERCALL_FINALIZE allows for callin an user-supplied subroutine named finalize_problem at the end of simulation.
  May be useful to output a summary of the run or some problem-specific data that is not handled by other means.
#endif
#undef USERCALL_FINALIZE
