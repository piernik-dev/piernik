#define HDF5
#define IONIZED
#undef  MAGNETIC

#undef ISO

#define GLOBAL_FR_SPEED

#define GRAV

#if !(defined(MULTIGRID) || defined(POISSON_FFT))
#define MULTIGRID
#warning You have not defined neither MULTIGRID nor POISSON_FFT gravity. Assuming MULTIGRID.
#endif

#if 0
  Dirty trick!
  The following declaration is used in multigrid.F90 to remove density bias for some test cases
#endif
#define JEANS_PROBLEM
#define RTVD
